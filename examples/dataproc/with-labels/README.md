# Dataproc Cluster with Labels

Dataproc cluster with GCP labels applied for environment, team, and cost-centre attribution, enabling cost reporting and resource filtering in the GCP Console.

## Usage

```bash
terraform init -backend=false
terraform validate
```
