# Dataproc Cluster with Custom Network

Dataproc cluster placed in a specific VPC network and subnet, enabling network isolation, private IP configuration, and integration with on-premises connectivity.

## Usage

```bash
terraform init -backend=false
terraform validate
```
