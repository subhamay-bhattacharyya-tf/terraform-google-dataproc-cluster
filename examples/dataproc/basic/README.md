# Basic Dataproc Cluster

Minimal Dataproc cluster using all default settings: 1 master node, 2 worker nodes, `n1-standard-4` machine type, and `2.1-debian11` image.

## Usage

```bash
terraform init -backend=false
terraform validate
```
