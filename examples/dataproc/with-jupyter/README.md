# Dataproc Cluster with Jupyter

Dataproc cluster with the JupyterLab optional component enabled and HTTP port access exposed, allowing interactive notebook-based data exploration via the Dataproc web UI.

## Usage

```bash
terraform init -backend=false
terraform validate
```
