# High Availability Dataproc Cluster

Dataproc cluster with a 3-node master configuration for high availability. The HA setup eliminates the master as a single point of failure.

## Usage

```bash
terraform init -backend=false
terraform validate
```
