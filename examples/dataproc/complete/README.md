# Complete Dataproc Cluster

Fully-featured Dataproc cluster exercising all available configuration axes: HA masters, high-memory workers, preemptible secondary workers, Jupyter and Zeppelin components, custom VPC placement, a dedicated service account, idle auto-delete, and resource labels.

## Usage

```bash
terraform init -backend=false
terraform validate
```
