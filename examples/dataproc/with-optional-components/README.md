# Dataproc Cluster with Optional Components

Dataproc cluster with both JupyterLab and Apache Zeppelin notebook components enabled, providing multiple options for interactive data exploration and query development.

## Usage

```bash
terraform init -backend=false
terraform validate
```
